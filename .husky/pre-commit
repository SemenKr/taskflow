#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🎯 Запуск проверок перед коммитом..."

pnpm lint-staged || exit 1

echo "✅ Проверки завершены, коммит создается!"
